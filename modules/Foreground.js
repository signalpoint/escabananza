import { Layer } from './Layer.js'

export class Foreground extends Layer {

  constructor({ x, y, image, scrollSpeed, scrollDirection, autoScroll }) {

    super({ x, y, image, scrollSpeed, scrollDirection, autoScroll })

  }

}
